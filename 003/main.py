# The prime factors of 13195 are 5, 7, 13 and 29.

# What is the largest prime factor of the number 600851475143 ?

prime_numbers = []

# def is_prime(n):
#   range_limite = int(n / 2 + 1)

#   for i in range(2, range_limite):
#     if n % i == 0:
#       return False
  
#   return True

# def largest_prime_factors(n):
#   range_limite = int(n / 2 + 1)
  
#   divisors = [x for x in range(2, range_limite) if n % x == 0]

#   return max(filter(is_prime, divisors))

# print((largest_prime_factors(600851475143)))

